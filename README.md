# TCP-Checker-Server

This is a light API server for checking port(s) of IP(s) from `tcping`

# Requirement 

- Tcping (`apt install tcping`)
- Python 3+

# Additional
- Systemd (Recommended)
- Supervisor